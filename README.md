## Additions

### Search is fully implemented
- Looks for text in the title, category, brand, and description that matches the search and only displays those items

### Removed node_modules, and added it to gitignore
- Should make cloning/deploying faster since Docker already runs *npm install*

## To-Do
- Improve UI/UX for screen readers
